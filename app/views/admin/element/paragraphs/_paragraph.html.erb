<%= simple_format(paragraph.content) %>

<span class="meta"><%= link_to "Edit", edit_admin_page_element_paragraph_url(@page, paragraph.id), :class => 'fancybox fancybox.ajax' %> | <SPAN id="move">MOVE</SPAN> | <%= link_to "Delete", admin_page_element_paragraph_url(@page, paragraph.id), method: 'delete', confirm: 'Are you sure you want to delete this?', :remote => true %></span>